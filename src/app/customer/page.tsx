'use client';\n\nimport { SERVICES } from '@/lib/constants';\nimport Link from 'next/link';\nimport { FaArrowRight } from 'react-icons/fa';\n\nexport default function CustomerHome() {\n  const services = [\n    { key: 'lunches', icon: '🍱', color: 'bg-orange-100' },\n    { key: 'pulpas', icon: '🥤', color: 'bg-yellow-100' },\n    { key: 'pulque', icon: '🍺', color: 'bg-amber-100' },\n    { key: 'travel', icon: '✈️', color: 'bg-blue-100' },\n  ];\n\n  return (\n    <div className=\"pt-8\">\n      <div className=\"bg-gradient-coral-teal rounded-2xl p-8 text-white mb-12\">\n        <h1 className=\"text-3xl font-bold mb-2\">¡Bienvenido a HOME!</h1>\n        <p className=\"text-lg opacity-90\">Descubre productos y experiencias increíbles</p>\n      </div>\n\n      <div className=\"grid grid-cols-2 md:grid-cols-4 gap-4 mb-12\">\n        {services.map((service) => {\n          const serviceData = SERVICES[service.key as keyof typeof SERVICES];\n          return (\n            <Link\n              key={service.key}\n              href={`/customer/category/${service.key}`}\n              className={`${service.color} rounded-xl p-6 text-center hover:shadow-lg transition transform hover:scale-105 cursor-pointer`}\n            >\n              <div className=\"text-4xl mb-2\">{service.icon}</div>\n              <h3 className=\"font-bold text-primary-dark mb-1\">{serviceData.name}</h3>\n              <p className=\"text-xs text-gray-700\">\n                {service.key === 'lunches' && '$35-50'}\n                {service.key === 'pulpas' && '$500'}\n                {service.key === 'pulque' && '$27-49'}\n                {service.key === 'travel' && 'Múltiples'}\n              </p>\n            </Link>\n          );\n        })}\n      </div>\n\n      <div className=\"bg-white rounded-2xl p-8 shadow-lg\">\n        <h2 className=\"text-2xl font-bold mb-6 gradient-text\">Categorías Destacadas</h2>\n        <div className=\"space-y-4\">\n          {services.map((service) => (\n            <Link\n              key={service.key}\n              href={`/customer/category/${service.key}`}\n              className=\"flex items-center justify-between p-4 hover:bg-primary-light rounded-xl transition\"\n            >\n              <div className=\"flex items-center gap-4\">\n                <span className=\"text-3xl\">{service.icon}</span>\n                <div>\n                  <h3 className=\"font-bold text-primary-dark\">{SERVICES[service.key as keyof typeof SERVICES].name}</h3>\n                  <p className=\"text-sm text-gray-600\">{SERVICES[service.key as keyof typeof SERVICES].description}</p>\n                </div>\n              </div>\n              <FaArrowRight className=\"text-primary-coral\" />\n            </Link>\n          ))}\n        </div>\n      </div>\n\n      <div className=\"mt-12 bg-blue-50 border-l-4 border-primary-teal rounded-lg p-6\">\n        <h3 className=\"font-bold text-primary-dark mb-2\">📍 Zona de Cobertura</h3>\n        <p className=\"text-gray-700 mb-2\">Entregamos en: <strong>León, Guanajuato</strong></p>\n        <p className=\"text-gray-700 mb-2\">Horario: <strong>Lunes a Domingo</strong></p>\n        <p className=\"text-sm text-gray-600\">¿Preguntas? Contáctanos por WhatsApp: <strong>+524772045944</strong></p>\n      </div>\n    </div>\n  );\n}\n